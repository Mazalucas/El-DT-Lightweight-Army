# Video masters (demasiado pesados para Git; el README usa MP4 comprimido + release)
assets/*.mov

# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/
env/

# Environment
.env
.env.local
.env.*.local
modules/cerebro-app/.firebaserc
modules/cerebro-app/functions/.env
modules/cerebro-app/functions/.env.local
modules/cerebro-app/functions/.env.cerebro-prime-a0729
modules/cerebro-app/src/.env.development.local
modules/cerebro-app/src/.env.production.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
*.egg-info/
.eggs/

# Logs
*.log
logs/

# Vitals — local only (plantillas en repo; valores reales no viajan por Git)
vitals/workspace.yaml
vitals/ops/session.yaml
vitals/ops/dt-upstream-state.md

# Borradores personales temporales (si usás vitals/work/inbox/)
vitals/work/inbox/**/draft-*

# Credenciales
*.credentials
client_secret*.json
**/google-credentials.json
**/google-token.json

# Marketing — contexto por proyecto (skill product-marketing)
.agents/product-marketing.md

# Módulos — datos fiscales / personales locales (NIF, IBAN, facturas emitidas)
modules/*/assets/local/
modules/**/assets/local/**

# App Facturas — proyecto local (no versionado; datos fiscales + código)
apps/facturas/

# Módulos — cola local de captura (chat → app)
modules/*/.local/
modules/**/.local/**

# Atelier — contexto de diseño unificado (skill design-context)
.agents/design-context.md
.agents/design-system/

# Demos temporales con marca (Atelier) — no versionar en plantilla
.cursor/skills/design/templates/preview/examples/
.agent/skills/design/templates/preview/examples/

# Generaciones locales (nunca en plantilla clonable)
output/
temporal/
tools/**/node_modules/
tools/**/out/

# Impeccable — cache local del detector
.impeccable/hook.cache.json
