---
name: indexar-reuniones
description: "[Módulos] Actualizar manifest de reuniones Meet sin descargar contenido. Use when the user invokes /indexar-reuniones."
---

# indexar-reuniones

Solo escaneo → `modules/cerebro-profesional/.local/manifest.jsonl`.

```bash
cd modules/meet-notes-sync && npm run scan
```

Informar cuántos archivos y si hay `config.yaml` con `sourcePath` correcto.
