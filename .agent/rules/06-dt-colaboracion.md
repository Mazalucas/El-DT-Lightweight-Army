# Colaboración DT (sesión e identidad)

Espejo de `.cursor/rules/06-dt-colaboracion.mdc`.

- Sin sesión válida → pedir **`/yo`** antes de escribir o commitear.
- Sesión solo con **`/yo`**; **`/actualizar`** no toca sesión.
- `roster.yaml` y `roles.yaml` sin participantes/roles de ejemplo en la plantilla; roles libres o lista definida por el proyecto.

`DOC-REF-001` · `DOC-OV-004`
