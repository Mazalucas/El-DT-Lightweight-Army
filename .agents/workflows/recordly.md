---
description: "[Video] Demo del producto corriendo — Recordly graba la pantalla; el binario queda fuera del git."
dt_command: recordly
---
# /recordly

**Grupo:** Video
**En una frase:** Demo del producto corriendo — Recordly graba la pantalla; el binario queda fuera del git.
**Cuándo:** Screen recording, walkthrough o GIF del flujo real. /recordingly es el mismo command.
**Quién:** Cualquier operador del repo.

Ejecutá el skill **`recordly`** — `.cursor/skills/recordly/` y `.agents/skills/recordly/`.

_Generado desde `vitals/config/commands-meta.yaml` — corré `scripts/sync-commands-from-meta.sh`._
