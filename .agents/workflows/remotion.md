---
description: "[Video] Asset de video en React — plantilla, props, lote, render."
dt_command: remotion
---
# /remotion

**Grupo:** Video
**En una frase:** Asset de video en React — plantilla, props, lote, render.
**Cuándo:** El carril de tools/video/ROUTING.md es Remotion: composición que se mantiene, parametriza o renderiza en lote.
**Quién:** Cualquier operador del repo.

Ejecutá el skill **`remotion-producer`** — `.cursor/skills/remotion-producer/` y `.agents/skills/remotion-producer/`.

Delegá en el subagente **`remotion-producer`** (`.cursor/agents/remotion-producer.md`) vía Task (`subagent_type: remotion-producer`) con el alcance del usuario. Si el IDE no expone subagentes (Antigravity, Codex), ejecutá el pipeline de la skill en esta conversación.

_Generado desde `vitals/config/commands-meta.yaml` — corré `scripts/sync-commands-from-meta.sh`._
