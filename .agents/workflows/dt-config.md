---
description: "[Rutina] Elegir qué reglas van en cada mensaje. El perfil queda en esta PC."
dt_command: dt-config
---
# /dt-config

**Grupo:** Rutina del día a día
**En una frase:** Elegir qué reglas van en cada mensaje. El perfil queda en esta PC.
**Cuándo:** Querés más o menos contexto del DT, o ver qué está siempre activo y qué entra solo cuando hace falta.
**Quién:** Cualquier operador del repo.

Ejecutá el skill **`dt-config`** — `.cursor/skills/dt-config/` y `.agents/skills/dt-config/`.

_Generado desde `vitals/config/commands-meta.yaml` — corré `scripts/sync-commands-from-meta.sh`._
