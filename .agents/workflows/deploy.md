---
description: "[Trabajo con el framework] Desplegar la app — primera vez autocompleta scripts y config del repo."
dt_command: deploy
---
# /deploy

**Grupo:** Trabajo con el framework
**En una frase:** Desplegar la app — primera vez autocompleta scripts y config del repo.
**Cuándo:** Querés publicar a staging o producción; tras bootstrap genera vitals/config/deploy.yaml.
**Quién:** Cualquier operador del repo.

Ejecutá el skill **`deploy`** — `.cursor/skills/deploy/` y `.agents/skills/deploy/`.

_Generado desde `vitals/config/commands-meta.yaml` — corré `scripts/sync-commands-from-meta.sh`._
