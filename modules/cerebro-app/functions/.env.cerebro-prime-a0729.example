# Cloud Functions — proyecto cerebro-prime-a0729 (no versionar con valores reales)
# Copy to: modules/cerebro-app/functions/.env.cerebro-prime-a0729
# Secrets (GOOGLE_OAUTH_CLIENT_SECRET, ENCRYPTION_KEY) → firebase functions:secrets:set

GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_PICKER_API_KEY=
APP_URL=https://cerebro-prime-a0729.web.app

# Invitaciones org (Resend) — opcional en dev (link en logs + UI admin)
MAIL_API_KEY=
MAIL_FROM=noreply@tudominio.com

# Producción: firebase functions:secrets:set INVITE_TOKEN_PEPPER
# INVITE_TOKEN_PEPPER=
