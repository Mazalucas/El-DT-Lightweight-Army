---
dt_command: hack
group: work
group_title: "Trabajo con el framework"
tagline: "Auditoría de seguridad del proyecto — mentalidad de atacante, entrega defensiva (SEV + prioridad)."
skill: hack-audit
---

# /hack

**Grupo:** Trabajo con el framework
**En una frase:** Auditoría de seguridad del proyecto — mentalidad de atacante, entrega defensiva (SEV + prioridad).
**Cuándo:** Querés cazar vulnerabilidades, auth/permisos, API, secrets, superficie de agentes/IA o riesgos de arquitectura en el propio repo.
**Quién:** Cualquier operador del repo.

Ejecutá el skill **`hack-audit`** — `.cursor/skills/hack-audit/` y `.agents/skills/hack-audit/`.

Delegá en el subagente **`hack-audit`** (`.cursor/agents/hack-audit.md`) vía Task (`subagent_type: hack-audit`) con el alcance del usuario. Si el IDE no expone subagentes (Antigravity, Codex), ejecutá el pipeline de la skill en esta conversación.

_Generado desde `vitals/config/commands-meta.yaml` — corré `scripts/sync-commands-from-meta.sh`._
