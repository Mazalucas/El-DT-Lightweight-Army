# Auth starter — copy to active project

Minimal auth shell. Pair with `patterns/auth-flows`.

## Estructura sugerida

```text
src/
  components/auth/
    AuthLayout.tsx
    LoginForm.tsx
  pages/login.tsx
  styles/auth.css
```

## Requirements

- Labels on all inputs (no placeholder-only)
- Focus visible states
- Error message slot
- `./scripts/atelier-detect.sh` before ship
